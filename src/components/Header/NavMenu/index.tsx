import React from 'react';
import './index.css';

import { NavButtonsList } from '../Navbar/NavButtonsList';

export const NavMenu = () => {
  return(
    <div className="nav-menu">
      <NavButtonsList />
    </div>
  )
}