export interface Work {
  id: string,
  name: string,
  description: string,
  tools: string[],
  srcIcon: string,
  screenshots: string[]
}

export interface Contacts {
  title: string,
  url: string
}